[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
# All tests live in tests/. Without this, a bare `pytest -q` recurses into build
# artifacts (src-tauri/sidecar/ ships matplotlib's own test suite inside the
# PyInstaller bundle) and collection dies silently.
testpaths = tests
