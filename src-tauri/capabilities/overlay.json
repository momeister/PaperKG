{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Permissions for the AI-Cursor overlay window (R1).",
  "windows": [
    "overlay"
  ],
  "permissions": [
    "core:default"
  ]
}
